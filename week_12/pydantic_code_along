# # exxample

def get_fellows_names(fellow: list[str]) -> list[str]:
    return fellow


def get_averge(name: str, scores: list[float]) -> float:
    avearage_score = sum(scores)/len(scores)
    print(f"{name}'s average score is {avearage_score}")
    return avearage_score


def greet(name: )